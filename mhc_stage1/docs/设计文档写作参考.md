# 6 特性详细功能设计

## 6.1 计算块划分（刘一辰、陈伍洋、邵怡阳、付琦峰）---包括VF融合部分的

![](https://wiki.huawei.com/vision-file-storage/api/file/download/upload-v2/WIKI2026042210863540/42535987/75435f65df734ab0ac9d755abc48010a.png)

包括 C/V 类型判别、Cube Block 划分、Vector Block 划分、指令重排四个一级模块。Vector Block 划分中包含多个二级模块。

该 Pass 的输入输出规则概括如下，细节请参阅详细模块介绍：

1. Core Type 的判断，基于 dot op 沿 def-chain 传播 cube 标签。没有被传播到 cube 标签的算子被标记为 vector，同样会向上传播。
2. 划分是 mlir block 级别的操作，会遍历每一个 mlir block，来进行计算块的划分：因此不会出现 op 跨越 mlir block 被分到同一个计算块中的情况。
3. Vector 划分：Vector 结果被 Cube 使用时，该 Vector 计算块仅包含必要的前置计算。其余 Vector会在不成环的情况下完全融合。
4. Cube 划分：Cube 划分仅依赖数据块上的连通性，所有在数据依赖图中联通的 Cube 块会被划分到同一个计算块中。不存在数据依赖的 Cube 不会被划到同一个计算块中；跨越 V 边界的 C 不会被融合到一起；
5. 计算块 Core Type：所有计算块中一定只包含 Cube 或只包含 Vector 的 Core Type。
6. 指令重排基于划分出来的计算块，即基于 ssbuffer.block_id 进行重排。重排过程中遵循原则：​**只做等价变换**​。并保证所有 ssbuffer.block_id 相同的 op 被排到相邻的位置。
7. 控制流/BlockOp/函数调用: 包含 op 的语句例如 scf.for, scf.if, scf.call 等会被分配单独且唯一的 block_id 但该 block_id 不表示任何计算块划分策略，仅为了输出一致性；如果有多返回值的话，会分配对应的 Core Type。格式为 ssbuffer.core_type = "cube,cube,vector,cube" （假设返回值有 4 个且依次为 cube, cube, vector, cube）

### 6.1.2 C/V 类型判别

C/V 类型判别：根据 IR 识别每个 Op 需要放在 Cube 还是 Vector 核上执行，以及每个 Value 是在 cube 还是 vector 上，并进行标记。

具体流程为：

1. 1. 种子节点标记：遍历并寻找所有必定在 Cube 上执行的 Op，标记为种子。
   2. 种子节点会基于 def 链条向上传播，通过 pattern 识别来自 GM 的数据来源，并将整个链条上可以在 Cube 上运算的 Op 标记为 Cube.
   3. 种子节点接下来会基于 use 链条向下传播，通过 pattern 识别写入 GM 的输出结果，并将整个链条上可以在 Cube 上运算的 Op 标记为 Cube.
   4. Cube 标记完毕后，会将所有剩余的节点标记为 Vector，并再次执行向上传播，此时会沿着 def 链条，将所有遍历到的 Op 添加 Vector 标签，如果该 Op 已经被搭上了 Cube 标签，则将标签改编为 CubeAndVector
   5. 在标记完成后，为了使 Op 的标签能够直接确定在哪个核上执行，该流程中会将所有被标注为 CubeAndVector 的标签分裂成两个 Op，分别持有 Cube 和 Vector 标签，并根据后续调用点的类型指向对应的 Op

### 6.1.3 Cube Block 划分

给定一个有向无环图 G=(V,E) ，其中节点集合 V 包含三类节点：Vector 节点（即分块障碍节点）、 Cube节点（A⊆V, 需要分块的点）、dot 节点（B⊆A, 即具有特殊语义的锚节点）。目标是将 A 划分为若干不相交的块 {P1,P2,…,Pk}，满足以下约束：

1.DAG 保持性：对每个块内节点进行缩点后，所得图 G′仍为 DAG；
2.dot 唯一性：每个块 Pi​ 最多包含一个 dot 节点；
3.依赖内聚性：dot 节点所依赖的上下游节点应尽可能归入同一块；
4.最小化块数：在满足上述约束的前提下，k尽可能小；

算法分为两步：首先进行 dot锚点驱动的分块算法，再进行残差节点层序归并算法，流程图如下：

![image](https://wiki.huawei.com/vision-file-storage/api/file/download/upload-v2/WIKI2026042210863540/43193032/7d720e1306f84b77b3a138b9e462a591.png)

### 6.1.4 Vector Block 划分

根据第一层生产的标记，将同层的 Vector 计算合理地划分为多个 Block.

**模块时序图：**

![](https://wiki.huawei.com/vision-file-storage/api/file/download/upload-v2/WIKI2026033010593816/41574403/e6ed2973fda84e66826439b8c9f0f4c9.png?1775184989756)

BFS实现流程图：

![image](https://wiki.huawei.com/vision-file-storage/api/file/download/upload-v2/WIKI2026042210863540/43192912/ca10667c3fc64e498b07fa3186927ac0.png)

**剪枝模块**

经过融合算法挑选出来的 VF 都需要执行后处理优化模块，该模块的主要作用为对当前模块的算子进行剪枝优化，因为算法在融合的过程中无法具有全局的视角，后处理主要即补足全局视角对融合的限制。当前后处理仅包含一个基于 C/V 分界线的剪枝模块，因此此处暂且不做后处理策略管理模块的冗余设计，仅实现该剪枝策略。
该剪枝策略即：当 V 的融合跨越某个 C 的边界，将一些本不需要一定在 C 之前执行的 V 融合进需要在 C 之前执行的 VF 中时，该模块会将这些可以放在 C 后面执行的 V 从当前 VF 中去除，以保证流水不会劣化。
具体场景如下：

![image](https://wiki.huawei.com/vision-file-storage/api/file/download/upload-v2/WIKI2026042210863540/43192952/6f5e4cbfeb594c719b6063af6ffee673.png)
框架伪代码：

```cpp
void cutErrorOp(Block *block, SmallVector<Operation *> &nowFuseGroup, DenseMap<Operation *, bool> &visited,
                SmallVector<Operation *> &candidates, DenseMap<Operation *, int> &indegree,
                const  MemoryDependenceGraph &memGraph)
{
    // 1.Find ops in fuse group whose next node is a non-fusable (CUBE-only) op
    auto toProcess = findOpsAdjacentToCube(block, nowFuseGroup, visited, memGraph);

    // 2. early return: if cannot find one node which next node is CUBE, need to check if return or not;
    if (cannot find one node which next node is CUBE) {
        return;
    }

    // 3. Collect keepOps transitively (data + memory + loop-carried deps)
    auto keepOps = collectKeepOps(block, toProcess, nowFuseGroup, memGraph);

    // 4. Remove non-kept ops from fuseGroup and restore BFS state
    evictAndRestoreState(block, keepOps, nowFuseGroup, visited, candidates, indegree, memGraph);
}

```
## 6.2 插入核间同步（刘茹枫、丁一）

![](https://wiki.huawei.com/vision-file-storage/api/file/download/upload-v2/WIKI2026033010593816/41787729/d9c201bc58594e4bbbce48134d3d4272.png?1775648076601)

### 6.2.1 数据依赖分析模块（刘茹枫）

![](https://wiki.huawei.com/vision-file-storage/api/file/download/upload-v2/WIKI2026033010593816/41695406/9ed18f28a72243e8bb9425464e9f2eda.png?1775554572444)

获取计算块之间的数据依赖关系，包括c->v/v->c/c->c三种（该部分不考虑v->v之间的数据依赖）

该模块输入：带有`ssbuffer.core_type`和`ssbuffer.id`信息的ir

该模块输出：计算块信息，v-c依赖关系信息，c-v依赖关系信息，c-c依赖关系信息

#### 6.2.1.1 block信息统计

统计计算块信息`BlockInfo`，所有计算块信息存为`BlockInfoMap`，用于后续依赖关系分析

**​注：​**这一部分中，也会统计`scf.for`、`scf.if`等控制流作为特殊计算块的信息

#### 6.2.1.2 外部输入分析（v->c分析）

遍历`blockInfoMap`，分析每个cube计算块的所有inputs：

* 如果input属于cube，则进行c->c特殊情况分析；
* 如果input属于vector，进行producer/consumer层级关系分析，确定producer&consumer的blockid，存为`DependencyInfo`，加入`V2CDependency`。

#### 6.2.1.3 外部输出分析（c->v分析）

遍历`blockInfoMap`，分析每个cube计算块的所有outputs：

* 如果output属于cube，则进行c->c特殊情况分析；
* 如果output属于vector，进行producer/consumer层级关系分析，确定producer&consumer的blockid，存为`DependencyInfo`，加入`C2VDependency`。

#### 6.2.1.4 c->c特殊情况分析

![](https://wiki.huawei.com/vision-file-storage/api/file/download/upload-v2/WIKI2026033010593816/41556510/04c02ab20a64487793e664ab493429de.png?1775137626782)

cube计算块之间可能会存在如上图所示的两种依赖关系：①乘数依赖前面的dot（dependindex为0/1）；②加数依赖前面的dot（dependindex为2）

#### 6.2.1.5 producer/consumer层级关系分析

该部分是为了保证在处理依赖关系时，生产者和消费者​**处于同一层级**​，保证后续数据搬运和同步操作的正确性。

![](https://wiki.huawei.com/vision-file-storage/api/file/download/upload-v2/WIKI2026033010593816/41674644/662b5b3e9e9945969943ed422611820a.png?1775545839261)![](https://wiki.huawei.com/vision-file-storage/api/file/download/upload-v2/WIKI2026033010593816/41675135/f2ee5d6840f6488c8bd01ec57eef9029.png?1775545970876)![](https://wiki.huawei.com/vision-file-storage/api/file/download/upload-v2/WIKI2026033010593816/41675202/712536c4b672459294611aa41560b5ec.png?1775546005719)![](https://wiki.huawei.com/vision-file-storage/api/file/download/upload-v2/WIKI2026033010593816/41738694/8bb8c9a2cac04bdf88a4c100e8d9e92d.png?1775619168247)![](https://wiki.huawei.com/vision-file-storage/api/file/download/upload-v2/WIKI2026033010593816/41740392/891bd70b4e634b0fb81e1a414bb876e5.png?1775619503068)

* 如果producer和consumer在同一mlir block中，则直接用生产和消费的op所在的blockid作为producerBlockId和consumerBlockId
* 如果producer和consumer在不同mlir block中，则​**找到最小的公共层级**​，选取op在这一层级所属的blockid作为producerBlockId/consumerBlockId，另一op选取同层级的控制流op的blockid作为producerBlockId/consumerBlockId

### 6.2.2 插入数据搬运（刘茹枫）

![](https://wiki.huawei.com/vision-file-storage/api/file/download/upload-v2/WIKI2026033010593816/41697543/b60039d9d2274cb8802f26beda645c1b.png?1775556444419)

根据6.2.1获取的依赖关系，分别针对c->v/v->c/c->c的依赖关系插入数据搬运。

具体包括插入点分析以及插入具体搬运操作两个部分，插入搬运操作可根据不同代际进行扩展。

数据搬运操作包括生产者传出数据、消费者接收数据、分配数据存放的buffer三个部分。

#### 6.2.2.0 计算块起始/结束op获取(公用)

获取计算块开始和结束的op，用于后续确定插入点位置

#### 6.2.2.1 插入点位置

1. 生产者传出数据：在producerblock结尾
2. 消费者接收数据：在consumerblock开头
3. 存放数据的buffer：跟随生产者传出数据以及消费者接收数据的位置

根据依赖关系中blockid和`BlockInfo`中的`*startOp`/`*endOp`可获取1、2对应位置。

#### 6.2.2.2 数据搬运

##### **（1）V->C**

遍历`V2CDependency`，对于每个DependencyInfo，首先通过6.2.2.1获取插入点位置，后分别插入内存分配、V block传出数据、C block接收数据的相关OP。

* **​内存分配：​**L1（`cbuf`）上分配内存，`memref.alloc()`标签为`#hivm.address_space<cbuf>`
* **​V block传出数据：​**将数据从nd布局转为nz布局，并通过`hivm.hir.copy`传出
* ****C block接收数据：****`hivm.hir.convert_layout`将数据转回nd布局，并通过`memref.memory_space_cast`+`bufferization.to_tensor`将L1上的memref转为tensor

##### **（2）C->V**

遍历`C2VDependency`，对于每个DependencyInfo，首先通过6.2.2.1获取插入点位置，后分别插入内存分配、C block传出数据、V block接收数据的相关OP。

* **​内存分配：​**UB上分配内存，`memref.alloc()`标签为`#hivm.address_space<ub>`
* **​C block传出数据：​**通过`hivm.hir.fixpipe`传出
* ****V block接收数据：****通过`memref.memory_space_cast`+`bufferization.to_tensor`将UB上的memref转为tensor

##### **（3）C->C**

遍历`C2CDependency`，对于每个C2CDependencyInfo，首先判断依赖模式：

如果是乘数依赖，通过6.2.2.1获取插入点位置，后分别插入内存分配、C block传出数据、C block接收数据的相关OP。

* **​内存分配：​**L1上分配内存，`memref.alloc()`标签为`#hivm.address_space<cbuf>`
* **​C block传出数据：​**通过`hivm.hir.fixpipe`传出
* ****C block接收数据：****通过`memref.memory_space_cast`+`bufferization.to_tensor`将UB上的memref转为tensor

如果是加数依赖，不做处理。

#### 6.2.2.3 nd2nz

（1）将2D按ND排布的数据转为4D按NZ排布的数据

（2）对非整数倍shape的数据做泛化处理

#### 6.2.2.5 给新增op打上标签

1. 新增op按op所在的计算块的标签添加Block ID及CUBE/VECTOR标签。
2. 同一组数据传输的op会用全局唯一的`TransferID`标记。

### 6.2.3 flag id管理（公用）

功能：返回能够使用的flag_id

![](https://wiki.huawei.com/vision-file-storage/api/file/download/upload-v2/WIKI2026033010593816/41788758/8273fc2582c842bbbcc0c38b525fe27e.png?1775649107100)

#### 6.2.3.1 获取现有 Flag ID 最大值

目的：为需要生成新同步点的场景提供基础计数，确保新生成的 `<span data-vt-id="oxcx3eio">flag_id</span>` 全局唯一，避免与现存同步点冲突。

输入：funcOp

输出：返回当前范围内未被使用的最小正整数 ID

#### 6.2.3.2 同步点复用分析

判断是否有与当前同步点控制区域不冲突的同步点id，如果有，则返回id，否则返回-1

如果某一组同步点被完全包含于与当前block无关的scf.for或scf.if中，则可以复用该组同步点的flag_id。

### 6.2.4 同步插入模块（刘茹枫）

该模块目的是保证L1/UB上，写入时原有数据已被使用，读取时需要的数据已被写入，防止踩内存
![](https://wiki.huawei.com/vision-file-storage/api/file/download/upload-v2/WIKI2026033010593816/41784248/b2b690fdf4124cea9ec28e2847322e11.png?1775642566946)

#### 6.2.4.2 同步阻塞分析

根据数据流c->v/v->c，给set、wait加上正确的阻塞标签

6.2.4.3 给新增op打上标签

逻辑同6.2.2.5：

1. 新增op按op所在的计算块的标签添加Block ID及CUBE/VECTOR标签。
2. 同一组数据传输的op会用全局唯一的`TransferID`标记。

#### 6.2.4.3 主逻辑：插入同步点

![](https://wiki.huawei.com/vision-file-storage/api/file/download/upload-v2/WIKI2026033010593816/41788825/ddb5d574061e46ff827760bc522fab75.png?1775649224501)

**执行流程：**

遍历依赖list：

1. 根据`DependencyInfo`和`blockInfoMap`获取producer和consumer计算块各自的起止点`producerStartOp`、`producerEndOp`、`consumerStartOp`、`consumerEndOp`
2. 调用`analyzeSyncReuse`分析是否存在可以复用的flag_id
3. 如果有可复用flag_id，使用该flag_id，否则调用`acquireNextAvailableFlagId`获取新的flag_id
4. 判断flag_id是否合法，不合法则回退整个DAG，合法则插入同步点
5. 插入防止读冲突的同步点：在producer fixpipe/copy op后插入set op，consumerStartOp前插入wait op
6. 插入防止写冲突的同步点：在producer fixpipe/copy op前插入wait op，consumerEndOp后插入set op
7. 插入起始/结束的同步点：在for循环外，前后分别插入set op和wait op

### 6.2.5 分scope（丁一）

复制删除方案

![](https://wiki.huawei.com/vision-file-storage/api/file/download/upload-v2/WIKI2026042210863540/42536009/de7efe87002a47b296baa7fb532e68a9.png)

#### 6.2.5.1创建cube和vector的scope

将所有的操作分别放到cube和vector的scope结构当中，并且打上对应的标签

#### 6.2.5.2归一化处理

![](https://wiki.huawei.com/vision-file-storage/api/file/download/upload-v2/WIKI2026042210863540/42536030/8161775ff45b44a8a86e985b52d10470.png)

遍历规则

遍历scope当中的所有op

1.当参数的core_type与当前Scope类型不匹配时  添加op到需处理的列表中并且加上可以直接删除的标签

2.对于带region控制流 分别检测该操作本身，输出结果和所有region内操作 包含与当前scope类型不匹配参数  加入到需处理的列表中打上需归一化的控制流标签

控制流处理规则

处理步骤：

1. 遍历结果：检查每个结果的core_type
2. 识别不匹配：`resultCoreType != scopeType`
3. 替换terminator use：将yield中对不匹配结果的引用替换为常量占位值
4. 递归处理Region：对控制流内部的嵌套操作重复上述过程

### 6.2.5 优化（预留）

优化数据搬运&同步Op的插入位置，减少对Vector计算的打断/核间等待时间等

### 6.2.6 预计输出

annotation.mark

memref依赖

ptr

![](https://wiki.huawei.com/vision-file-storage/api/file/download/upload-v2/WIKI2026033010593816/41909249/0c1162180ffe45eca16b0cbf27f0a850.png?1775805151526)

其中，绿色和蓝色的op会分别标上唯一的transferID

### 6.1.5 指令重排

根据前两层划分的 Block ID，对 IR 进行指令重排，使相同 Block ID 的指令在 IR 上连续，便于后续进行同步或多缓冲区处理。

![](https://wiki.huawei.com/vision-file-storage/api/file/download/upload-v2/WIKI2026033010593816/41554490/4e532114277048fa8c0a5979971a1e28.png?1775135222920)



5.4 基于CostModel的VF融合方案
 
5.4.1 功能概述
David V100增加了Out of Order (OoO)机制，VF内通过指令乱序队列有效提升指令执行效率，同时硬件Loop通过硬件unroll的方式填充指令乱序执行队列，进而显著提升指令执行效率。但指令乱序队列长度有限（64），若发射的指令数超过 issue queue 的承载能力，则会发生反压； VCADD等部分指令需要用Slide Unit，使得占用相同的resource的指令无法双发，容易造成空泡； VDIV等部分指令throughput > 1，会导致相同指令需要间隔throughput cycle才能发射，容易造成空泡。所以需要通过CostModel合理将指令划分到不同的VF中，从而达到Vector计算指令最大并行度。
 
当前NPUIR内置“应融尽融”的唯一融合策略，无法满足所有场景的需要，所以需要独立抽象一层PASS，提供不同的VF融合的子图划分策略。  
 
5.4.2 新增SR清单
系统需求编号	系统需求	系统需求描述
SR20260424729704	基于CostModel VF融合	1. 构建CostModel能力，解决存在超过10+的连续element wise op的算子的融合问题
2. 使能VF Fusion能力，支持MaxParallel模式作为默认配置

5.4.3 实现思路
（1）通过独立VFFusion Pass将独立抽象支持不同的costmodel和搜索遍历算法，VFFusion Pass负责将“合适”数量的op outline到同一个func当中，后续Pass可以将其融合成一个VF中，max-parallel的CostModel作为一种策略内置到里面。
图5-1 VFFusion pipeline diagram
 

（2）CostModel将根据cycle、throughput、exc_unit等指令信息，判断op间是否适合融合。
 
5.4.4 实现设计
 
5.4.4.1 VFFusion Pass 架构设计
图5-2 VFFusion pass sequence diagram
 

1. VFFusionPass 根据融合模式调用tryToFuse()，遍历Region->Block->Operation，若操作满足RegionBranchOpInterface接口，则递归进入其Region。针对当前Region的每一个block,执行块级融合。
2. FusionKindBase调用analyzeBlockImpl()分派至VFFusionAnalyzer进行融合分析。
3. VFFusionAnalyzer进行具体的块级融合分析：
    3.1 初始化opToIndex映射，将当前block内的所有Ops填入opsInBlock，并初始化并查集。
    3.2 遍历Block中的每一个Op，并遍历该Op的每一个操作数，根据不同融合模式子类的Analyzer执行具体的isFusiableImpl(),判断当前Op与其生产者是否可融合。若可融合，则在并查集中添加两个算子的索引。
    3.3 根据并查集最终的组划分情况，创建VFFusionBlockList。
4. FusionKindBase调用VFFusionOutliner，将分析出的VFFusionBlock提取为独立函数。
5. VFFusionOutliner进行函数替换，在原Block中删除旧Ops，插入对新提取函数的func.call。
5.4.4.2 MaxParallel cost model方案设计
图5-3 逻辑模型
 

MaxParallel接入VFFusion框架
1. 新增MaxParallelFusionKind类，继承自FusionKindBase类，负责调用analyzer分析ir，得到融合的划分；
2. 新增MaxParallelAnalyzer类，继承自VFFusionAnalyzerBase类，负责遍历ir，并通过costmodel判断是否融合，将融合后的ir归为一组；
3. 新增CostModelInfoBase类和CostModelInfo类，负责提供cycle、throughput、exc_unit等指令信息，作为costmodel的输入。
 
（2）MaxParallel算法流程
 
融合Cost Model：设计目标与整体方案
设计目标：给定HW_LOOP1和HW_LOOP2，根据Cost Model判定是否执行循环深融合。
根据乱序队列并行度优化方法可以看出，LOOP执行的实际并行度和迭代内指令依赖链长度有关，我们定义DepChainLen(loop)
 
可以得到Cost整体方案如下图所示
 
这里解释一下SubCostModel0的作用，VF乱序执行窗口实际执行的并发度和迭代内数据依赖链长度(DepChainLen)相关，在DepChainLen长度不变的情况下增加计算指令数，将会变相提升计算并行度。所以 是Loop融合后指令并行度提升的充分条件；对于 的情况，表明Loop1和Loop2之间数据存在数据依赖，这种情况下有较大概率融合后导致并行度下降，需要引入额外的SubCostModel进行判定。
SubCostModel 1：存储瓶颈消除
对于 的情况，由于Loop1和Loop2之间存在数据依赖，相互融合可以消除部分UB LD/ST，但是同时会引入计算并发度下降。从直观上，如果Loop1+Loop2融合后（但是消除重复UB LD/ST前）特征为访存瓶颈，那么访存消除将带来性能提升。所以SubCostModel1的目标便是判断Loop1+Loop2在未消除重复UB LD/ST时是否为访存瓶颈。
本SubCostModel1中采用均摊cycle法判定是否为访存瓶颈，也就是计算在理想并发度的情况下，对比IO和Compute的均摊cycle，如果IO均摊cycle大于Compute均摊cycle，则判定为访存瓶颈：
 
IOScore计算比较简单，这里假定LD/ST不存在bankconflict，由于LD有两个Port，ST只有一个Port，所以IOScore计算方式如下：
 
ComputeScore计算方式比较复杂，由于不同计算指令具有不同的throughput和exu_num，这将影响单个循环的均摊cycle产生不同的占比影响。
 
 
SubCostModel 2：端口利用率提升
如果迭代内依赖链变长，同时还无法通过融合Loop消除访存瓶颈，接下来就是考虑端口利用率问题了。有两种情况下，将会导致计算的端口利用率无法占满：
1.	由于exu_num=1，导致指令只能通过一个exu发射，空余cycle无法被填满，例如cpt_set = {vcadd(exu_num=1), vcadd(exu_num=1), vadd(exu_num=2)}
 
2.	由于throughput>2，导致端口间隔发射指令，空余cycle无法被填满，例如cpt_set = {vdiv(tput=4), vadd(tput=1)}
 
由此，我们可以通过计算合并前后端口利用率是否获得提升，来决策是否融合，伪代码如下：
 
PortEffciency用于计算理论端口利用率，这里可以复用部分ComputeScore计算中的函数；
 
SubCostModel 3：指令并行度不下降
指令并行度不下降SubCostModel旨在聚合一些HW_LOOP的同时确保指令并行度不下降。可是为什么需要这个SubCostModel呢？
回顾一下SubCostModel1（存储瓶颈消除），在计算IOScore过程中，我们按照没有bankconflict的方式计算，这样子本质上是对UB LD/ST开销的一种低估。在实际应用SubCostModel1过程中，易导致一些在“存储瓶颈边缘的”HW_LOOP落单，影响端到端性能。
例如循环：
 
如果src/dst没有bank conflict，则并不是存储瓶颈型；反之如果src/dst存在bank conflict，则LD/ST成为瓶颈。
同时，由于计算端口有限，尽管依赖链延长会导致循环的计算并行度下降，但是如果并行度仍可以将物理端口满载，则适当的延长依赖链并不会导致性能下降，反而可以有助于消除上述的“存储瓶颈边缘的”HW_LOOP。可是如何评估依赖链延长是否会影响整体性能呢？
在HW_LOOP循环执行稳态过程中，通常指令执行在不同迭代下呈现如下趋势：
 
为了简化模型，按照指令均匀场景，则蓝色部分面积为64（乱序窗口大小），可得到指令下发的并行度估计为 。这其中包含LD/ST和计算指令，通过指令占比进一步估算计算指令并行度为 。根据硬件背景里提到的指令上限并行度的计算方式，可以得到，如果满足融合后：
 
 
则可以较大信心确保融合后性能不下降。同时消除“落单的存储瓶颈边缘的”HW_LOOP。
5.4.5 接口设计
接口名称	接口描述	使用方式
--enable-vf-fusion	使能编译流程执行VFFusion pass	bishengir-compile --enable-vf-fusion=True
--vf-fusion-mode="max-parallel"	使能VFFusion pass中选择融合模式为max-parallel 融合	bishengir-compile --vf-fusion-mode="max-parallel"
enable_vf_fusion	TA层使能编译流程执行VFFusion pass	enable_vf_fusion=True
vf_fusion_mode="max-parallel"	TA层选择融合模式为max-parallel融合	vf_fusion_mode="max-parallel"